import src.data
import src.features
import src.models
import src.charts
import src.utils

# This is used in the FlowPic code, data loading, and model training/predicting.
INPUT_SHAPE = (1500, 1500, 1)